<nav class="navbar navbar-dark bg-inverse">
  <ul class="nav navbar-nav">
    <li class="nav-item active">
      <%= link_to 'Home', home_path, class: "btn btn-primary", class: "nav-link" %>
    </li>
    <li class="nav-item dropdown">
      <a class="nav-link dropdown-toggle" href="#" id="supportedContentDropdown" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">Category</a>
      <div class="dropdown-menu" aria-labelledby="supportedContentDropdown">
        <% @categories.each do |cat| %>
          <%= link_to "#{cat.name}", book_category_path(:id => cat.id), class: "dropdown-item" %>
        <%end%>
      </div>
    </li>
    <li class="nav-item">
      <a class="nav-link" href="#">Orders</a>
    </li>
    <li class="nav-item">
      <%= link_to "Contacts", contact_path, class: "nav-link" %>
    </li>
    <li class="nav-item">
      <%= link_to "About", about_path, class: "nav-link" %>
    </li>
  </ul>
  <!-- <form class="form-inline float-xs-right">
    <input class="form-control" type="text" placeholder="Search">
    <button class="btn btn-outline-success" type="submit">Search</button>
  </form> -->
  <%= form_tag(book_search_path, :method => "get", id: "search-form", class: "form-inline float-xs-right") do %>
  <%= text_field_tag :search, params[:search], placeholder: "Search Books", class: "form-control" %>
  <%= submit_tag "Search", class: "btn btn-outline-success" %>
  <% end %>
</nav>
